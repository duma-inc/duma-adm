apiVersion: v1
kind: Service
metadata:
  name: duma-adm
  namespace: duma
  labels:
    app: duma-adm
spec:
  type: NodePort
  selector:
    app: duma-adm
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30318
